extends layout

block content
    p= text.session_timed_out_redirect

block scripts
    script(type='application/javascript')
        | window.setTimeout(function(){
        |   window.location.href = "/login";
        | }, 2000);